{
  "name": "ebuddy-technical-test",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@repo/shared && npm run build --workspace=@repo/backend && npm run build --workspace=@repo/frontend",
    "dev:all": "npm run dev:shared & npm run dev:backend & npm run dev:frontend",
    "dev:shared": "npm run dev --workspace=@repo/shared",
    "dev:backend": "npm run dev --workspace=@repo/backend",
    "dev:frontend": "npm run dev --workspace=@repo/frontend",
    "install:all": "npm install && npm run install:shared && npm run install:backend && npm run install:frontend",
    "install:shared": "npm install --workspace=@repo/shared",
    "install:backend": "npm install --workspace=@repo/backend",
    "install:frontend": "npm install --workspace=@repo/frontend"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "turbo": "^2.4.1",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
